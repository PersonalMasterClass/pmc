
	<tr>
		
		<td class="col-md-1">
			<%= if availability.monday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Monday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.tuesday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Tuesday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.wednesday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Wednesday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.thursday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Thursday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.friday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Friday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.saturday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Saturday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-1">
			<%= if availability.sunday
				 link_to availability.times_string, new_booking_path(:presenter_id => availability.presenter.id, :availability => availability, :day => "Sunday"), method: :get, :class=>"btn btn-xs btn-success" else "-" end %>
		</td>

		<td class="col-md-4" >
			<% if current_user.presenter == availability.presenter %> 
				<%= link_to 'Remove', presenter_availability_path(current_user.presenter, availability),
					data:{confirm: 'Are you sure?'}, method: :delete, :class=>"btn btn-danger" %>
				<%= link_to 'Edit', edit_presenter_availability_path(availability.presenter, availability), :class=>"btn btn-warning" %>				
			<% end %>
		</td>
	</tr>

